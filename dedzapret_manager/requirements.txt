# Core dependencies
pyyaml>=6.0.0
psutil>=5.9.0
requests>=2.28.0

# Optional UI dependencies
pystray>=0.19.0
Pillow>=9.0.0

# Development dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
flake8>=5.0.0
mypy>=1.0.0

# Windows specific dependencies (optional)
# Note: These are runtime dependencies, not Python packages
# - winws2.exe (DPI desynchronisation runtime)
# - WinDivert.dll (Network driver)
# - WinDivert64.sys (Network driver)
# - sing-box.exe (Proxy/VPN client)
